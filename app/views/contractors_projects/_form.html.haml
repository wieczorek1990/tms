= form_for @contractors_project, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :contractor_id, :class => 'control-label'
    .controls
      = f.collection_select :contractor_id, Contractor.all, :id, :name, :class => 'text_field'
  .control-group
    = f.label :project_id, :class => 'control-label'
    .controls
      = f.collection_select :project_id, Project.all, :id, :name, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), contractors_projects_path, :class => 'btn'
